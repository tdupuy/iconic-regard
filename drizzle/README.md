# Database baseline

The canonical database schema for this project is defined in src/lib/db/schema.ts.

Legacy migration files were removed so the schema stays the single source of truth.
No automatic database changes are applied from this repository.
